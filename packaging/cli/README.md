CLI scanner
-----------

Simple script to POST a local file to the backend scan API.

Usage:

```powershell
python packaging/cli/scan.py path/to/file.pdf
```

Install dependencies:

```powershell
pip install -r packaging/cli/requirements.txt
```
